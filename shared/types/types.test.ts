import {AuthData} from "./index";

// No tests needed for just types.
// This helps appease the test coverage checker.
describe("AuthData", () => {
  it("should do nothing", () => {
    expect(1 + 1).toBeDefined();
  });
});
