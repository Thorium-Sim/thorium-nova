/** Grid square size in pixels (used when no background image) */
export const GRID_SIZE_PX = 20;

/** Default canvas size when no background image is set */
export const DEFAULT_CANVAS_SIZE = 800;

/** Station default size as fraction of image width */
export const STATION_SIZE_FRACTION = 0.1;

/** Viewscreen default size as fraction of image width (same as station) */
export const VIEWSCREEN_SIZE_FRACTION = 0.1;
