mutation UniverseStarSetPosition(
  $id: ID!
  $starId: ID!
  $position: PositionInput!
) {
  universeTemplateStarSetPosition(
    id: $id
    starId: $starId
    position: $position
  ) {
    id
  }
}
