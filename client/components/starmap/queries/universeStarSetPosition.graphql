mutation UniverseStarSetPosition(
  $id: ID!
  $starId: ID!
  $position: PositionInput!
) {
  universeTemplateSystemSetPosition(
    id: $id
    starId: $starId
    position: $position
  ) {
    id
  }
}
