mutation UniverseSystemSetDescription(
  $id: ID!
  $systemId: ID!
  $description: String!
) {
  universeTemplateSystemSetDescription(
    id: $id
    systemId: $systemId
    description: $description
  ) {
    id
  }
}
