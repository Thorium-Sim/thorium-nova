mutation UniverseSystemSetDescription(
  $id: ID!
  $systemId: ID!
  $description: String!
) {
  pluginUniverseSystemSetDescription(
    id: $id
    systemId: $systemId
    description: $description
  ) {
    id
  }
}
