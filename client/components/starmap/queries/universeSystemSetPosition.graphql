mutation UniverseSystemSetPosition(
  $id: ID!
  $systemId: ID!
  $position: PositionInput!
) {
  universeTemplateSystemSetPosition(
    id: $id
    systemId: $systemId
    position: $position
  ) {
    id
  }
}
