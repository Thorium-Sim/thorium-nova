mutation UniverseSystemSetPosition(
  $id: ID!
  $systemId: ID!
  $position: PositionInput!
) {
  pluginUniverseSystemSetPosition(
    id: $id
    systemId: $systemId
    position: $position
  ) {
    id
  }
}
