mutation UniverseSatelliteSetDistance(
  $id: ID!
  $objectId: ID!
  $distance: Float!
) {
  pluginUniverseSatelliteSetDistance(
    id: $id
    objectId: $objectId
    distance: $distance
  ) {
    id
  }
}
