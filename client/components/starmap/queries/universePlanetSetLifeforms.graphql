mutation UniversePlanetSetLifeforms(
  $id: ID!
  $objectId: ID!
  $lifeforms: String!
) {
  pluginUniversePlanetSetLifeforms(
    id: $id
    objectId: $objectId
    lifeforms: $lifeforms
  ) {
    id
  }
}
