mutation UniversePlanetSetLifeforms(
  $id: ID!
  $objectId: ID!
  $lifeforms: String!
) {
  universeTemplatePlanetSetLifeforms(
    id: $id
    objectId: $objectId
    lifeforms: $lifeforms
  ) {
    id
  }
}
