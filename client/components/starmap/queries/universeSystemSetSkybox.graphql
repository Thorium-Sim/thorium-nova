mutation UniverseSystemSetSkybox(
  $id: ID!
  $systemId: ID!
  $skyboxKey: String!
) {
  pluginUniverseSystemSetSkyboxKey(
    id: $id
    systemId: $systemId
    skyboxKey: $skyboxKey
  ) {
    id
  }
}
