mutation UniverseSystemSetSkybox(
  $id: ID!
  $systemId: ID!
  $skyboxKey: String!
) {
  universeTemplateSystemSetSkyboxKey(
    id: $id
    systemId: $systemId
    skyboxKey: $skyboxKey
  ) {
    id
  }
}
