mutation UniverseStarSetDescription(
  $id: ID!
  $starId: ID!
  $description: String!
) {
  universeTemplateSystemSetDescription(
    id: $id
    starId: $starId
    description: $description
  ) {
    id
  }
}
