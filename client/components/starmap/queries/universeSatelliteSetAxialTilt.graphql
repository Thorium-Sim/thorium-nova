mutation UniverseSatelliteSetAxialTilt(
  $id: ID!
  $objectId: ID!
  $axialTilt: Float!
) {
  pluginUniverseSatelliteSetAxialTilt(
    id: $id
    objectId: $objectId
    axialTilt: $axialTilt
  ) {
    id
  }
}
