mutation UniverseSatelliteSetShowOrbit(
  $id: ID!
  $objectId: ID!
  $showOrbit: Boolean!
) {
  pluginUniverseSatelliteSetShowOrbit(
    id: $id
    objectId: $objectId
    showOrbit: $showOrbit
  ) {
    id
  }
}
