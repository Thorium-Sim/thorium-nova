mutation ThrustersSetDirectionMaxSpeed(
  $pluginId: ID
  $outfitId: ID
  $shipId: ID
  $speed: Float!
) {
  thrustersSetDirectionMaxSpeed(
    pluginId: $pluginId
    outfitId: $outfitId
    shipId: $shipId
    speed: $speed
  ) {
    id
  }
}
