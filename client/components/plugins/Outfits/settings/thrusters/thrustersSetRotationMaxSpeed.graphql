mutation ThrustersSetRotationMaxSpeed(
  $pluginId: ID
  $outfitId: ID
  $shipId: ID
  $speed: Float!
) {
  thrustersSetRotationMaxSpeed(
    pluginId: $pluginId
    outfitId: $outfitId
    shipId: $shipId
    speed: $speed
  ) {
    id
  }
}
